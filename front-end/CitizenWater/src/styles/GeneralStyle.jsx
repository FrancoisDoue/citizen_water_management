import { StyleSheet } from 'react-native';
import styleColors from './StyleColors';

const generalStyle = StyleSheet.create({    
    view: {
        backgroundColor: styleColors.whiteTab,
        flex:1,
        padding: 10
    }
})

export default generalStyle;
